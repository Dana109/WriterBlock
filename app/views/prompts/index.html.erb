<h1>All Prompts</h1>
<div class = "well">
	<% @prompt.each do |prompt| %>
		
			<%= prompt.id %>
			<%= prompt.content %>
			<%= prompt.user %>
	<% end %>
</div>
<%= link_to 'Create a new prompt', 'prompts/new' %>